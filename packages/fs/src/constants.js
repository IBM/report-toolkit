import {constants as commonConstants} from '@report-toolkit/common';
const {SHORT_NAMESPACE} = commonConstants;

export const RC_FILENAME = `.${SHORT_NAMESPACE}rc.js`;
export const RC_NAMESPACE = SHORT_NAMESPACE;
